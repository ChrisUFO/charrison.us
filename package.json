{
  "name": "charrison-redirect",
  "version": "1.0.0",
  "description": "Minimal LinkedIn redirect",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --check index.js",
    "lint": "eslint .",
    "format": "prettier --write .",
    "prepare": "husky"
  },
  "lint-staged": {
    "**/*.js": [
      "eslint",
      "prettier --write"
    ],
    "**/*.{json,css,md,html}": [
      "prettier --write"
    ]
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "eslint": "^9.39.2",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.1"
  }
}
